FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir requests numpy
COPY simulator/ ./simulator/
CMD ["python", "simulator/data_stream.py", "--mode", "mixed", "--rps", "5"]
